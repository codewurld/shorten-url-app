<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Make My Links Short</title>
</head>

<body>
    <div class="container">

        <h1 style="margin-left: 250px; margin-top: 25px;">Make My Links Shorter than Mini-Me</h1>
        <div>
            <img src="./minimie.png" alt="mini me Austin Powers" style="height: 200px; margin-left: 500px;">
        </div>

        <!-- bootstrap styling add margin top & bottom on entire form  -->
        <!-- connect form to route with action -->
        <form action="/shortUrls" method="POST" class="my-4 form-inline ">
            <!-- make label class visible only for users with screen readers for accessibility purposes -->
            <label for="longURL" class="sr-only sr-only-focusable">Enter URL</label>
            <!-- add margin right to input -->
            <!-- remove default form setting -->
            <input required placeholder="Enter URL" type="url" name="longURL" id="longURL"
                class="form-control col mr-2">
            <button class="btn btn-danger" type="submit">Eeeee MINImiseME</button>
        </form>

        <!-- style table using bootstrap table-striped property -->
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Long URL</th>
                    <th>Mini URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <!-- loop through each url in miniMeUrl -->
            <tbody>
                <% miniMeUrls.forEach(miniMeUrl=> { %>
                    <tr>
                        <td><a href="<%= miniMeUrl.fullLink %>">
                                <%= miniMeUrl.fullLink %>
                            </a>
                        </td>

                        <td><a href="<%= miniMeUrl.shortLink %>">
                                <%= miniMeUrl.shortLink %>
                            </a>
                        </td>
                        <td>
                            <%= miniMeUrl.clicks %>
                        </td>

                    </tr>
                    <% }) %>

            </tbody>
        </table>
    </div>
</body>

</html>